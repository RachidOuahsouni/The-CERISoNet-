const express = require("express");
const router = express.Router();
const notificationsController = require("../controllers/notifications.controller");

// Ajoute ici des routes REST si besoin

module.exports = router;